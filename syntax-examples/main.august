@main

#link rust
#pragma build build

Task build:[test] {
  rust.build;
  my_cmd;
}

Task test {
  exec("cargo test");

}

cmddef my_cmd {
  print_file("Cargo.toml");
  set_env_var("var", "cont");
}
